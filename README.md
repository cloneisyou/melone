# melone

Public release feed for the Melone desktop app. Auto-update artifacts
(installers + `latest*.yml` / `beta*.yml` feeds) are published here by CI from
the private `cloneisyou/melone-mvp` source repo.
