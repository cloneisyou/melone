# PostHog product analytics (renderer). Same PostHog project as the web app;
# events are tagged with client=melone-desktop so the two stay filterable apart.
# The project token is a write-only public client key — safe to embed in the build.
# Leave unset to disable analytics entirely (no network calls, no console output).
VITE_POSTHOG_KEY=
